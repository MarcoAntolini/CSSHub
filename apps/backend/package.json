{
	"name": "@csshub/backend",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "npm run serve",
		"serve": "npm run vercel:prepare && vercel dev --listen 3000 --yes",
		"dev:all": "npm run dev",
		"vercel:prepare": "node scripts/sync-shared-dist.mjs",
		"pretypecheck": "npm run build --workspace @csshub/shared && npm run vercel:prepare",
		"typecheck": "tsc --build",
		"typecheck:all": "npm run typecheck"
	},
	"dependencies": {
		"@upstash/redis": "^1.35.6",
		"dotenv": "^17.4.2",
		"zod": "^3.24.4"
	},
	"devDependencies": {
		"@types/node": "^24.10.1",
		"@vercel/node": "^5.8.1",
		"typescript": "^5.9.3",
		"vercel": "^54.0.0"
	}
}
